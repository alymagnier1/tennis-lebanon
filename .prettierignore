pnpm-lock.yaml
**/.next/
**/.expo/
**/.turbo/
**/dist/
**/build/
**/node_modules/
supabase/migrations/*.sql

# Vendored Figma Make export kept as a design reference, not app source.
Figma designs/

# Agent worktrees are full checkouts of this repo. Without this, `pnpm format`
# reformats another session's uncommitted work and `format:check` reports the
# same file twice.
.claude/worktrees/

# Generated by Expo on install and gitignored. Nothing to format, and its
# presence made the pilot gate fail on a file nobody wrote.
apps/mobile/expo-env.d.ts
