# Handover — 2026-09-26

Picks up from [`HANDOVER_2026-09-24.md`](HANDOVER_2026-09-24.md), now superseded. Latest review: [`audits/CLAUDE_IMPLEMENTATION_ASSESSMENT_2026-09-25.md`](audits/CLAUDE_IMPLEMENTATION_ASSESSMENT_2026-09-25.md) — the reviewer approved the implementation; **release still waits on credential retirement and the two-phone rehearsal.** Release truth: [`STAGING_CHECKLIST.md`](STAGING_CHECKLIST.md). Revised after [`audits/IMPLEMENTATION_RECHECK_2026-09-26.md`](audits/IMPLEMENTATION_RECHECK_2026-09-26.md) — see _Corrections from the 09-26 recheck_.

**Where we are:** all code for the release-readiness fix list is merged and live. What remains is device verification (in progress — both test devices are signed in), retiring the leaked staging key, and the recorded rehearsal. No code task is blocking.

---

## State, with the IDs you will need

| Thing                   | Value                                                                                                                                                                                                                      |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Branch                  | `staging` @ `e6f7bf7` (PR #17, the 09-26 recheck fixes). Work on a branch off `staging`, PR into `staging`. `main` is stale; ignore it.                                                                                    |
| Vercel production       | Tracks **`staging`** (was `pilot-handover`, frozen since 09-12, until 2026-09-25). `racketbound.com` live, `www` → apex.                                                                                                   |
| Staging Supabase        | `rvdzalxavpcijbiikkjz`, migrations **001→108** in order                                                                                                                                                                    |
| `process-notifications` | v4: dedicated `PROCESS_NOTIFICATIONS_TOKEN` invoker token; DB calls on `SUPABASE_SECRET_KEYS.default` (`"keySource":"secret"` in its response); cron runs return 200                                                       |
| EAS build (installed)   | `168b2257-439f-4868-8acf-70f668009537`, commit `5c09b84`, version/runtime **`0.1.0`**, channel `staging`                                                                                                                   |
| Latest EAS update       | Android update ID **`01a0dcac-e8d8-75f6-94db-e69a2e1e7a5a`** (group `afbe7871…`), commit `e6f7bf7`, published 2026-09-26 with the bundle checked (§4). Includes the conflict-query fix. Previous: `01a0da8a…` (`9b698fb`). |
| Test devices            | Emulator `emulator-5554` (Android 15, Play services) = player **A / host**; founder's phone = player **B / joiner**. Both signed in. Expo Go was uninstalled from the emulator to free space.                              |
| Emulator push           | **Cannot register.** `supportsPush()` requires `Device.isDevice`; the emulator never writes a token row. Expected, not an FCM fault. Phase D needs a second physical phone.                                                |
| Push tokens             | `device_push_tokens` = **0**; T7 on the phone makes it **1**                                                                                                                                                               |
| Leaked key              | Staging legacy `service_role` JWT, exposed in a chat screenshot 2026-09-22. **Still valid.** Phase 1 done, phase 2 not.                                                                                                    |

## What changed since 09-24

- **Invite acquisition** — share links are `https://racketbound.com/invite#<token>` (token in the fragment, never sent to the server); `/invite` is a static, detail-free page with _Open in the app_ (Android intent with install fallback) and _Get the app_ (`NEXT_PUBLIC_GET_APP_URL` → build `168b2257`). A signed-out invitee returns to the invite after onboarding (`pending-invite.ts`).
- **Three invite defects from the 09-25 review** — migrations 107/108: a blocked caller gets `not_found` and nothing else; shared-link Decline is a no-op (addressed invites still record refusal; preview exposes `is_addressed`); `skill_out_of_range` / no-future-time → no Accept offered. pgTAP `108_…_test.sql` (24 assertions).
- **Keys, phase 1** — dashboard, build and EAS preview/production env use `sb_publishable_…`; the function uses the secret key; service key out of Vault and Vercel. Legacy keys can't be rotated on Supabase any more — see §7d of the checklist.
- **Runtime version** — `fingerprint` failed every build started from Windows (pnpm shortens folder names to 60 chars on Windows, 120 on Linux; the fingerprint hashes paths). Now **`appVersion`**, version `0.1.0`. **Bump `version` in `apps/mobile/app.json` on any native change**; JS-only ships with `eas update`.
- **Schedule-conflict warning** on create (advisory; publish never blocked).
- **Late-slot bug** — an 11:30 PM + 90 min slot ended before it started on create, reschedule, add-time and court confirmation. All four use `slotWindowUtc` now.
- **French** — 51 genuinely English strings translated.
- **E2E** — `testID`s on the loop's controls; `e2e/maestro/loop/` (publish → join → court confirmed). **Never executed.**
- **Settings → Build info** — runtime, channel, `downloaded update` vs `built-in bundle`, and the running update ID. This is how you prove which JS a phone runs.

## Corrections to 09-24

- "Invite links point at a dead domain" — fixed; the domain serves.
- "Push is dead" was two faults, not one: the **sender answered every cron call 401 since setup** (fixed), _and_ no device had registered (still true until T7). The "576 successful runs" were 401s.
- 106 is applied. `db:reset` / `db:test` pass `--local` (`2c4f7b3`).

## Corrections from the 09-26 recheck

- **Conflict warning leaked across accounts.** The query was keyed by time window only and kept fresh for 30 s, and sign-out clears only the profile query, so a second account on the same running app could see the first account's conflict. Now keyed by user ID and disabled until someone is signed in (`src/lib/agreed-time-conflicts-query.ts`, with an account-switch test on a real `QueryClient`). Clearing every user-scoped query at sign-out is a separate, still-open review.
- **The emulator cannot register for push.** An earlier note here said it could. It can't: see _Emulator push_ above. The two-token gate needs two physical phones.
- **The create screen publishes fixed-time matches only.** The rehearsal asked for a flexible match and an agreed-time push, neither of which the app can produce today. Phase D now follows the fixed journey; flexible voting needs its own decision.
- **Accepting a shared invite is not a join request.** It says nothing about a host approving one; T27 covers that path.
- **Signing out and in is not a session refresh.** T3 (fresh sign-in) and T3b (refresh) are recorded separately.

## Gotchas on this machine — read before running anything

- **Avast HTTPS scanning** re-signs TLS. Node CLIs (eas-cli, npx) fail with `unable to verify the first certificate`. `%USERPROFILE%\windows-root-ca.pem` is **stale** (Avast regenerated its root after 09-17). Export the current root from `Cert:\LocalMachine\Root` (subject _Avast Web/Mail Shield Root_) to a PEM and set `NODE_EXTRA_CA_CERTS` to it. Git uses schannel and is fine. Large EAS uploads sometimes reset mid-way — retry once.
- **Publishing an `eas update` safely** (checklist §4, follow exactly): updates do **not** read `eas.json` build-profile env and **do** read local `.env` files. Export with `EXPO_NO_DOTENV=1`, the `build.staging.env` values set explicitly (or via `eas env:exec preview`), and **`--clear`** — without it Metro reused a bundle with the old key inlined. Inspect the `.hbc` (staging URL and `sb_publishable_` present; no `eyJ…` JWT, no `127.0.0.1`), then `eas update --skip-bundler --input-dir dist --channel staging --platform android --environment preview`.
- **Local `.env` files point at staging.** `apps/mobile/.env.local.off` is the local-DB variant; copy it to `.env.local` only temporarily and delete it before any export.
- **A local tool rewrites working-tree files with CRLF** (seen 09-22 20:59, 09-25 01:47). No content change; `.gitattributes` normalises on commit. `pnpm format` clears it. It also once left a stale `.git/index.lock`.
- **Git Bash mangles `/data`-style paths** passed to `adb shell` — set `MSYS_NO_PATHCONV=1`.
- **Prettier is non-idempotent** on inline code wrapped across lines inside Markdown list items. Keep each command on one line.
- **Never** paste or log keys. Never type passwords into forms on the testers' behalf. Never edit an applied migration — add a new one (latest is 108).
- Repo auto-merge is disabled on GitHub; merge PRs after all 4 checks pass (Lint/typecheck/unit, Supabase migration smoke test, Vercel, Vercel Preview Comments).

---

## Next, in order

Report each test by ID. Database checks are marked 🔎.

### Phase A — the device runs the right code

- **T1** (both): Settings → bottom shows `Runtime 0.1.0 · staging · downloaded update` and `Update 01a0dcac-e8d8-75f6-94db-e69a2e1e7a5a`. No extra line = still on the embedded bundle, and `01a0da8a…` = the previous update; fully close and reopen (the first launch downloads, the next runs it).

### Phase B — compatibility check before retirement

- **T3** fresh sign-in: sign out and in on the phone.
- **T3b** session refresh (not the same as T3): leave the phone signed in past the access-token lifetime (1 h at the default), bring the app to the foreground, open a match hub without being asked to sign in; 🔎 Auth logs show a successful `refresh_token` grant for that user then. Never log the tokens.
- **T4** open a match hub on the emulator · **T5** emulator shares an invite, phone opens it via the page → _Open in the app_ → invite screen · **T6** edit the bio and change it back.
- **T7** the **phone** enables Profile → Notifications and allows the prompt; 🔎 `device_push_tokens` 0 → **1**. The emulator stays at none — expected (see _Emulator push_), and not a reason to delay Phase C.
- **T8** 🔎 `net._http_response` 200s.

### Phase C — retire the leaked key (checklist §7d; don't wait for the rehearsal)

- **T9** Supabase → API Keys: deactivate legacy `anon` and `service_role`.
- **T10** JWT Keys: Migrate JWT secret → Rotate.
- **T11** wait the access-token lifetime **+ 15 min** (1 h 15 min at the default), then Revoke the legacy secret. Record the cutoff; review logs for privileged activity 2026-09-22 → cutoff.
- **T12** repeat T3, T3b, T4–T6 and T8; confirm the retired key is rejected through a controlled check, never by pasting it.

### Phase D — two-player rehearsal (checklist §7c), recorded

**Needs two physical phones**, both on the latest update and both with notifications on (🔎 two token rows). The emulator can coordinate but cannot receive a push. The create screen only makes **fixed-time** matches, so there is no voting step.

**Partial run 2026-09-26** (phone A + emulator B): see [`audits/REHEARSAL_PARTIAL_2026-09-26.md`](audits/REHEARSAL_PARTIAL_2026-09-26.md). Proved: T1, create/share/Accept, **T18 OS join push on A**, court confirm on both hubs, shared + addressed Decline, out-of-level, offline recovery on A, T27 request/approve + A join-request push. Still open for the gate: second physical phone (B court push / two tokens / B request-accepted push), cold install, blocked invite, attendance after start, Arabic RTL, unreachable summary + Sentry.

T13 conflict banner on create (needs update `01a0dcac…` running) · T14 host publishes a fixed-time singles match · T15–T17 shared invite: Decline leaves cleanly and the link still works, then Accept joins · **T18 the host's phone physically shows the "joined" push** (`match_participant_joined`) · **T19 WhatsApp hand-off, then court confirmed; the joiner's phone physically shows "Court confirmed"** (`match_court_confirmed`) · T20 attendance + result → rematch card · T21 data off/on recovers · T22 Arabic RTL · T23 🔎 unreachable-notification summary and Sentry. With a **new** account: T24 signed-out invite → sign-up → lands on the invite · T25 blocked player sees "not valid" and no details · T26 out-of-level player sees the level message. Separately: **T27** host publishes listed on Discover with "requires approval"; the other player requests from Discover; the host's phone shows the request, the host approves, the requester's phone shows the acceptance. Record build, update ID, device models/OS and every failure.

Not covered, and not to be ticked from this run: flexible time voting and the agreed-time push. The create screen cannot publish a flexible match; that needs a decision (out of cohort-1 scope, or a supported fixture tested separately).

Then run the Maestro loop once (`e2e/maestro/README.md`; needs Maestro installed and testers' credentials in their own terminal) and fix what it finds.

## Still open

- Checklist: partial §7c evidence in `audits/REHEARSAL_PARTIAL_2026-09-26.md`; full two-phone gate still open. "No open critical/high security findings" stays open until Phase C.
- Legal pages are development drafts; counsel review before real players.
- T27 joiner OS push and blocked-invite edge still open (request/approve on A proved 2026-09-26).
- Flexible voting: decision needed (see Phase D).
- Emulator false-offline: `isInternetReachable` null treated as offline — fix before trusting emulator network tests.
- Clear all user-scoped cached queries at sign-out, not just the profile query — a separate review.
- The APK is ~120 MB because the internal build carries four CPU architectures; a Play AAB (per-device split), dropping x86 and WebP images would shrink it. Not blocking.
- Prune plus-tagged staging users before cohort A.
- Optional: gate on committed content instead of the working tree; find the CRLF-rewriting tool; Android App Links; Expo SDK 57 after the pilot.
