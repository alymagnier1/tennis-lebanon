# Handover — 2026-09-26 (end of day)

Picks up from [`HANDOVER_2026-09-24.md`](HANDOVER_2026-09-24.md), now superseded. Release truth: [`STAGING_CHECKLIST.md`](STAGING_CHECKLIST.md) (31 ticked / 45 open, each tick with dated evidence). Rules: [`../CLAUDE.md`](../CLAUDE.md). Why things are the way they are: [`DECISIONS.md`](DECISIONS.md) (newest first).

## Start here

1. **Work on a branch off `staging`, PR into `staging`.** `main` is stale; ignore it. Repo auto-merge is disabled: merge only after all 4 checks pass (Lint/typecheck/unit, Supabase migration smoke test, Vercel, Vercel Preview Comments). Never commit onto a branch whose PR is already merged — start a new one from `origin/staging`.
2. **One thing is in flight — read _In flight_ below before publishing any EAS update.**
3. Read _Gotchas on this machine_ before running anything.

## Where things stand

- All code for the release-readiness fix list is merged and live. The leaked staging `service_role` key is **retired** (legacy JWT secret revoked 2026-09-26 15:47:19 Beirut) and the logs show it was never used.
- Push works end to end on a **locked** phone: high priority, sender every 3 minutes (confirmed 17:34:49 → 17:36:00 → on screen ~17:36).
- The **two-phone rehearsal (§7c) is waiting for a second physical phone.** A partial run with phone + emulator is recorded in [`audits/REHEARSAL_PARTIAL_2026-09-26.md`](audits/REHEARSAL_PARTIAL_2026-09-26.md).
- The **next EAS build is deliberately not started**: the founder wants UI changes first. It will be ≈ 65 MB instead of 116 MB (ARM-only, trimmed fonts, lossless WebP — already merged, #30).

## In flight: the chat restyle (PR #31)

- [PR #31](https://github.com/alymagnier1/tennis-lebanon/pull/31) (`chat-polish`, `ccc303a`): WhatsApp-style match chat and the fix for the gap between the message box and the keyboard. CI green. **Not merged** — waiting on the founder's look at it.
- **Its preview is live on the `staging` channel**: EAS update `01a0de5f-aba9-79e6-ad24-66830c427f69` (group `99ab5fef…`), published 18:40 from the PR branch. The emulator runs it (screenshots taken); the founder's phone gets it after two full restarts.
- **Therefore: do not publish an `eas update` from `staging` until #31 is merged or dropped** — it would silently take the new chat off the phones.
- If the founder approves: merge #31 (no republish needed; same code). If they want changes: commit to `chat-polish`, publish a new preview the same way (§4 procedure, message prefixed `PREVIEW`). If dropped: publish from `staging` with the §4 procedure.
- Not yet verified: the keyboard gap on a real phone. The emulator uses the computer's physical keyboard, so it only shows Gboard's floating toolbar, never the on-screen keyboard.

## Waiting on the founder

- Chat preview: look, and whether the message box now sits right on the keyboard (phone).
- Offline banner: leave the app in the background with the screen off for 20+ minutes, reopen — there should be no "You're offline" (fix in update `01a0de11…` and later; proven by tests, not yet on a device).
- **Decisions:** flexible time voting (out of cohort-1 scope, or a supported fixture tested separately — the create screen only makes fixed-time matches); **photos in chat** (asked 2026-09-26; chat is text-only end to end; recommended after the pilot — it needs a migration, a private per-match storage bucket enforced by RLS, EXIF/GPS stripping, compression and limits, a viewer, moderation, and push copy; the image picker is already in the build).
- A second physical phone for §7c.
- When to start the next EAS build.

## Next work that needs no second phone

- §7c items that phone + emulator can do: cold install (uninstall on the emulator, new account, open an invite link), blocked-player invite, attendance + score (a fixture starting soon), Arabic RTL (T22).
- Bug: reopening an old invite for a **cancelled** match says "already in this match" (membership is checked before match status).
- Review: clear every user-scoped cached query at sign-out, not just the profile query.
- Run the Maestro loop once (`e2e/maestro/README.md`; needs Maestro installed and the testers' credentials in their own terminal).
- Local `.env` files (root, `apps/mobile/.env`) still hold the dead legacy `anon` key; swap in the publishable key before running locally against staging.
- Before cohort A: prune plus-tagged staging users; counsel review of the legal pages (development drafts).

## State, with the IDs you will need

| Thing                   | Value                                                                                                                                                                                                                                        |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `staging`               | `242d8b5` (PR #30, APK size). Open: #31.                                                                                                                                                                                                     |
| Vercel production       | Tracks `staging`. `racketbound.com` live, `www` → apex; `/invite` follows a second link opened in the same tab (#25).                                                                                                                        |
| Staging Supabase        | `rvdzalxavpcijbiikkjz`, migrations **001→110**. Latest: `109` (push token follows the signed-in account), `110` (sender every 3 min).                                                                                                        |
| `process-notifications` | **v6** (2026-09-26 17:12): `priority: "high"` on every push; dedicated `PROCESS_NOTIFICATIONS_TOKEN` invoker; DB calls on `SUPABASE_SECRET_KEYS.default` (`"keySource":"secret"`); cron `*/3`, runs return 200.                              |
| Keys                    | Mobile and dashboard use the publishable key. Legacy API keys deactivated; legacy JWT secret revoked; current signing key ECC `74cd036a…`.                                                                                                   |
| EAS build (installed)   | `168b2257-439f-4868-8acf-70f668009537`, commit `5c09b84`, version/runtime **`0.1.0`**, channel `staging`, 116 MB.                                                                                                                            |
| Latest EAS update       | **`01a0de5f-aba9-79e6-ad24-66830c427f69`** — the PR #31 **preview**, not merged code. Last update from merged code: `01a0de11-da30-746a-be08-901954b2be78` (`3cfcf97`, 17:15).                                                               |
| Test devices            | Founder's phone = account `34c31b48…` (player A / host), the only active push token. Emulator `emulator-5554` (Android 15) = account `e209165b…` (player B); it cannot register for push (`Device.isDevice`). Account `68d6cf5c…` is unused. |
| Checklist               | 31 ticked / 45 open.                                                                                                                                                                                                                         |

**T1 (which code a phone runs):** Settings → bottom shows `Runtime 0.1.0 · staging · downloaded update` and `Update <id>`. An update downloads on one full launch and runs on the next — close and reopen twice.

## Gotchas on this machine — read before running anything

- **Avast HTTPS scanning** re-signs TLS. Node CLIs (eas-cli, npx) fail with `unable to verify the first certificate`. `%USERPROFILE%\windows-root-ca.pem` is **stale**. Export the current root from `Cert:\LocalMachine\Root` (subject _Avast Web/Mail Shield Root_) to a PEM and set `NODE_EXTRA_CA_CERTS` to it. Git uses schannel and is fine. Large EAS uploads sometimes reset mid-way — retry once.
- **Publishing an `eas update` safely** (checklist §4, follow exactly): updates do **not** read `eas.json` build-profile env and **do** read local `.env` files. Export with `EXPO_NO_DOTENV=1`, `EXPO_PUBLIC_APP_ENV=staging`, `EXPO_PUBLIC_AUTH_REDIRECT_URL=tennislebanon://auth/callback`, through `eas env:exec preview`, with **`--clear`**. Inspect the `.hbc` (staging URL and `sb_publishable_` present; no `eyJ…` JWT, no `127.0.0.1`), then `eas update --skip-bundler --input-dir dist --channel staging --platform android --environment preview`. Delete `dist` afterwards.
- **Runtime version is the app version** (`0.1.0`). Bump `version` in `apps/mobile/app.json` for a change to native code (a native dependency, the Expo SDK, a plugin that changes native code). The ARM-only ABI plugin does not need one (DECISIONS 2026-09-26). Never bump before the new build is installed — every phone would stop receiving updates.
- **`supabase functions deploy process-notifications --project-ref rvdzalxavpcijbiikkjz --use-api`** deploys without Docker. `supabase db push --linked` applies migrations (dry-run first). Never edit an applied migration — add a new one (latest is 110).
- **Docker Desktop is usually stopped**, so `pnpm db:test` does not run locally; CI's Supabase job runs the full pgTAP suite.
- **A local tool rewrites working-tree files with CRLF.** No content change; `pnpm format` clears it.
- **Git Bash mangles `/data`-style paths** passed to `adb shell` — set `MSYS_NO_PATHCONV=1`. Open a chat on the emulator directly: `adb shell am start -a android.intent.action.VIEW -d "tennislebanon:///match/<id>/chat" com.racketbound.app`. Screenshots: `adb exec-out screencap -p > shot.png`.
- **Prettier is non-idempotent** on inline code wrapped inside Markdown list items; run it twice.
- **Never** paste or log keys. Never type passwords into forms on the testers' behalf.

## Test list

Report each test by ID. 🔎 = database or log check.

- **Done 2026-09-26:** T1, T3 (fresh sign-in), T3b (session refresh, before and after the revoke), T4–T8, T9–T12 (key retirement and the post-revoke compatibility check), T18 (join push on A), T27 request/approve with A's push, and a push arriving on the **locked** phone.
- **Two-phone rehearsal (§7c)** — needs two physical phones, both on the latest update, a different account on each, notifications on (🔎 two token rows). The create screen only makes fixed-time matches, so there is no voting step. T13 conflict banner · T14 host publishes a fixed-time singles match · T15–T17 shared invite: Decline leaves cleanly and the link still works, then Accept joins · **T18** the host's phone shows the "joined" push · **T19** WhatsApp hand-off, court confirmed, and the joiner's phone shows "Court confirmed" · T20 attendance + result → rematch card · T21 data off/on recovers · T22 Arabic RTL · T23 🔎 unreachable-notification summary and Sentry. With a **new** account: T24 signed-out invite → sign-up → lands on the invite · T25 blocked player sees "not valid" and no details · T26 out-of-level player sees the level message. **T27** Discover + "requires approval": request, approve, and the requester's phone shows the acceptance. Record build, update ID, device models/OS and every failure.
- Not covered by the rehearsal, and not to be ticked from it: flexible time voting and the agreed-time push.

## What 2026-09-25/26 changed (details in DECISIONS)

- Invite links: `https://racketbound.com/invite#<token>`; the static page follows same-tab links; a signed-out invitee returns to the invite after onboarding. Invite preview matches acceptance and hides everything from a blocked player (107/108).
- Keys moved to publishable/secret; legacy keys retired; the sender authenticates with its own token.
- Push: token follows the account signed in on the phone (109); every push high priority; sender every 3 minutes (110).
- Sign-out ends this device's session only. The online state is settled by probing our backend and re-checked on every return to the foreground (the stale "You're offline" on the founder's phone).
- Schedule-conflict warning on create, scoped to the signed-in account; late-slot (11:30 PM + 90 min) fixed everywhere.
- APK size cut in code (next build): ARM-only, per-weight fonts with a lint rule, lossless WebP.
- Settings → Build info shows the running update. Runtime version = app version.
- E2E: `e2e/maestro/loop/` written, **never executed**.
